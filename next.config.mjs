/** @type {import('next').NextConfig} */
const nextConfig = {
  // Artifacts are small JSON/CSV/PNG read from Vercel Blob at request time.
  // No special config needed; kept explicit for clarity.
  reactStrictMode: true,
};

export default nextConfig;
